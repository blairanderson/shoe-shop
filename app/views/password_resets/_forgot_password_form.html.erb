<%= form_tag password_resets_path, method: :post do %>
  <div class="field">
    <%= text_field_tag :email, nil, placeholder: "email" %>
  </div>
  <div class="actions">
    <%= submit_tag "Reset my password!", class: "btn btn-inverse" %>
  </div>
<% end %>