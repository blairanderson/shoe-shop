<div class="container-fluid">
  <%= header "#{@valid_filters[@filter.to_s]} | #{@sort}", :a  %>
  <div class="navbar">
    <ul class="nav nav-pills">
      <% @valid_filters.each do |filter, title| %>
        <li class="sort-pill"><%= link_to title, filters_posts_path(@sort, filter) %></li>
      <% end %>
    </ul>
  </div>
</div>
