<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#current-user-nav">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= white_logo %>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="current-user-nav">
      <%= simple_form_for :search, method: :get, url: posts_path, html: {class: "navbar-form navbar-left"} do |f| %>
        <div class="form-group">
          <%= f.search_field :query, value: search_query, placeholder: "search... BETA", class: "form-control" %>
        </div>
      <% end %>
      <ul class="nav navbar-nav navbar-right">

        <% if current_user %>
          <li><%= link_to "New Pair", new_post_path %></li>
          <li><%= link_to "Profile", profile_path %></li>
          <li><%= link_to "Logout", logout_path %></li>
        <% else %>
          <li><%= link_to("Sign Up", signup_path) %></li>
          <li><%= link_to("Login", login_path) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>



