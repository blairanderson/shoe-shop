<%= simple_form_for(@post) do |f| %>
  <%= f.input :title %>
  <%= f.input :price, as: :tel %>
  <%= f.association :size, collection: Size.sort(name: :desc).all, prompt: "Choose a Size" %>
  <%= f.input :body %>
  <%= f.button :submit, class: "btn btn-inverse" %>
<% end %>
