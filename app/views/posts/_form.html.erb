<%= simple_form_for(@post) do |f| %>
  <%= f.input :title %>
  <%= f.input :price, as: :tel %>
  <%= f.association :size, collection: Size.order(name: :desc).load, prompt: "Choose a Size" %>
  <%= f.input :body %>
  <%= f.button :submit, class: "btn btn-inverse" %>
<% end %>
